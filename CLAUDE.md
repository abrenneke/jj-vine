## Coding

Your code should be as simple as possible. But not simpler.

Document fields on structs with ///.

## Linting

Use `cargo clippy` to lint your code.

## Commits

Never use `git`. Only use `jj`.